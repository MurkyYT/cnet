### GetSockAddress method of cnet::Socket

The **GetSockAddress** methods returns the address of the socket it-self

### Syntax
```C++
const Address GetSockAddress();
```

### Return value

The address of the socket