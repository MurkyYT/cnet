### Accept method of cnet::Socket

The **Accept** methods accepts a pending socket connection

### Syntax
```C++
std::shared_ptr<Socket> Accept();
```

### Return value

`std::shared_ptr<Socket>` with the accepted socket