### GetHandle method of cnet::Socket

The **GetHandle** methods returns the native handle to the socket

### Syntax
```C++
const SocketHandle GetHandle();
```

### Return value

The handle of the socket