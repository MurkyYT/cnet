### GetPeerAddress method of cnet::Socket

The **GetPeerAddress** methods returns the peer address the socket

### Syntax
```C++
const Address GetPeerAddress();
```

### Return value

The address of the peer