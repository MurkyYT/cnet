### Size method of cnet::Bytes

The **Size** methods returns the amount of bytes stored in the data

### Syntax
```C++
std::size_t Size();
```

### Return value
The amount of bytes inside the data vector