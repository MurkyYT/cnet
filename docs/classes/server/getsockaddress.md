### GetSockAddress method of cnet::Server

The **GetSockAddress** methods returns the address of the server it-self

### Syntax
```C++
const Address GetSockAddress();
```

### Return value

The address of the server