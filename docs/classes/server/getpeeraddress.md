### GetPeerAddress method of cnet::Server

The **GetPeerAddress** methods returns the peer address of the server

### Syntax
```C++
const Address GetPeerAddress();
```

### Return value

The peer address of the server