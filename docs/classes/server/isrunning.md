### IsRunning method of cnet::Server

The **IsRunning** method returns whether the server is running
### Syntax
```C++
bool IsRunning();
```

### Return value
`false` - the server is not running
`ture` - the server is running