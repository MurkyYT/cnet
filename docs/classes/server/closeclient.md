### CloseClient method of cnet::Server

The **CloseClient** method closes the specified client
### Syntax
```C++
void CloseClient(
    const Address& address
);
```

### Parameters

`address`

The address of the client