### GetPeerAddress method of cnet::Client

The **GetPeerAddress** methods returns the peer address of the client

### Syntax
```C++
const Address GetPeerAddress();
```

### Return value

The peer address of the client